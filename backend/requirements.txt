# Core API
fastapi
uvicorn[standard]
python-dotenv
python-multipart
pydantic

# Image handling (headless only — no GUI; keeps deploy under 512MB)
pillow
opencv-python-headless
numpy

# OCR (lightweight wrapper; needs Tesseract binary on system/Render)
pytesseract

# DB & auth
motor
pymongo
PyJWT
bcrypt